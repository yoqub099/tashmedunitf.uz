import ContactsAdmin from "@/components/templates/ContactsAdmin";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Filiallar — Admin" };

export default function FiliallarPage() {
  return <ContactsAdmin />;
}
