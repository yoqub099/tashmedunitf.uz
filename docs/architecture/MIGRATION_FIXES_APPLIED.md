# Migration Fixes Applied — 2026-06-05

Follow-up to `MIGRATION_VERIFICATION_REPORT.md`. The 6 residual P0/P1 build/CI items it flagged
are now **resolved and verified** (pnpm install + typecheck + production build, all green).

| #   | Item                                                             | Fix                                                                                                                                                                                                                                                       | Verification                                                                                                              |
| --- | ---------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 1   | `apps/admin` Dockerfile used `npm ci` (no lockfile → build FAIL) | New monorepo root-context `infrastructure/docker/images/Dockerfile.admin`; stale `apps/admin/Dockerfile.admin` removed                                                                                                                                    | `pnpm --filter @tmtu/admin build` → standalone at `apps/admin/.next/standalone/apps/admin/server.js` (matches Dockerfile) |
| 2   | `apps/web` Docker not monorepo-aware                             | New `infrastructure/docker/images/Dockerfile.web` (root context, `pnpm --filter @tmtu/web... build`); stale `apps/web/Dockerfile.frontend` removed; `compose.yml` + `ci.yml` contexts updated to repo root                                                | `pnpm --filter @tmtu/web build` → standalone layout matches Dockerfile COPY paths                                         |
| 3   | `packages/*` 100% unconsumed                                     | `@tmtu/utils` wired into `apps/web` + `apps/admin` (`workspace:*` dep + `transpilePackages` + `cn` re-exported from the shared package); `packages/config/*` registered in `pnpm-workspace.yaml` + root `workspaces`; `@tmtu/eslint-config` `files` fixed | `pnpm install --frozen-lockfile` ✓; `turbo typecheck` 11/11 ✓; web+admin production build ✓                               |
| 4   | CI e2e job started no servers                                    | `e2e` job now provisions postgres+redis services, PHP+composer, migrate+seed, builds & boots API (:8000) + web (:3000) + admin (:3001), `wait-on`, then runs Playwright                                                                                   | `ci.yml` e2e job                                                                                                          |
| 5   | `scripts/docker-safe.sh` called bare `docker compose`            | `export COMPOSE_FILE=…/infrastructure/docker/compose/compose.yml` added                                                                                                                                                                                   | `scripts/docker-safe.sh`                                                                                                  |
| 6   | Playwright artifact path mismatch                                | CI upload path → `e2e/report/` + `e2e/test-results/` (matches `playwright.config.ts` `outputFolder: 'report'`)                                                                                                                                            | `ci.yml`                                                                                                                  |

**Extras applied:** added `apps/web/src/app/api/health/route.ts` (compose healthcheck target); `.tool-versions` pnpm `9.15.0`→`10.28.2` (matches `packageManager`); `next-env.d.ts` added to root `.gitignore`.

**Notes for the maintainer:**

- The CI e2e seed uses `ADMIN_PASSWORD: Admin123456`; align this with the credentials the Playwright `auth-setup` logs in with.
- The new web/admin Docker images build at the repo root; the CI `docker` job builds them on every push to `main`, so any Docker regression is caught before deploy.
