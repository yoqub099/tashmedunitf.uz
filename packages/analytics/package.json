{
  "name": "@tmtu/analytics",
  "version": "0.1.0",
  "private": true,
  "description": "Shared analytics adapters — Google Analytics, Yandex Metrika, custom events",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'analytics: no lint' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
