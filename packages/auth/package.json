{
  "name": "@tmtu/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Shared auth helpers — token storage, refresh, guards",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'auth: no lint' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tmtu/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
