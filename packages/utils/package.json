{
  "name": "@tmtu/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities — date, format, validation, cn (class merger), etc.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo 'utils: TODO add eslint' && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tmtu/types": "workspace:*",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.5.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
